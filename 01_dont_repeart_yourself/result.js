/*
 * Taks: Put a ; at the end of each line.
 * Solution: 
 *   - Pressing A puts vim in insert mode at the end of the current line 
 *   - Add the ; by pressing ;
 *   - exit inset mode by presing <esc>
 *   - Move to the next line by pressing j
 *   - Repreat the A; sequence by pressing .
 */
var foo = 1;
var bar = 'a';
var foobar = foo + bar;
